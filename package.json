{
  "name": "logseq-syncthing-conflicts-helper",
  "title": "Syncthing Conflicts Helper",
  "description": "A Logseq plugin to help resolving Syncthing conflicts.",
  "author": "Rémi Aubel",
  "version": "0.0.5",
  "main": "index.html",
  "icon": "icon.png",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=iife --global-name=plugin",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.1"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.17",
    "diff": "^8.0.2"
  },
  "logseq": {
    "id": "logseq-syncthing-conflicts-helper",
    "icon": "icon.png"
  }
}
